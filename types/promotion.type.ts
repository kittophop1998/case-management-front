export type Promotion = {
  promotionCode: string;
  promotionName: string;
  promotionDetails: string;
  action: string;
  promotionResultTimestamp: string;
  period: string;
  eligibleCard: string[];
};
